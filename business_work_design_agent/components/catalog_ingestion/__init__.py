"""Langflow catalog-ingestion components.

Each component module is intentionally standalone and does not import from this
package.  This file exists only so Langflow can discover the category when the
directory is configured through ``LANGFLOW_COMPONENTS_PATH``.
"""
